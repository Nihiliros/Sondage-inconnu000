lister= document.getElementById('corpsliste');
var userlist=[];

function liste()
{
    userlist.appendChild(document.getElementsByClassName(#{user.fullname}));
    lister.appendChild(userlist);
}

liste();